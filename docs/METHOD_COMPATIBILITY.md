# Method Compatibility

| Family | Method | Foundation status | Stable output |
| --- | --- | --- | --- |
| PLS-SEM | PLS path modeling core | Release-qualified scoped Standard for deterministic recursive raw-data composite models with reflective Mode A, formative Mode B, single-item blocks, listwise deletion, and path, factor, or bounded PCA weighting. Cyclic and covariance-only models and bootstrap, permutation, prediction, group, PLSc, WPLS, and CB-SEM inference remain separate; no SmartPLS numerical identity is claimed. | Yes; current engine/archive/native/packaged evidence, selected-run XLSX export, and same-run save/reopen for the bounded `pls_pm_v1` scope |
| PLS-SEM | Inference/resampling | Percentile bootstrap, BCa, studentized/bootstrap-t, and jackknife support are validated for the documented bounded scope. Structural Path Randomization v1 is separately release-qualified as a scoped Standard for single-model direct structural-score paths using fixed original converged PLS scores, exchangeable reduced-model residuals, unadjusted pathwise plus-one probabilities, and the documented paired homoscedastic Gaussian constant-variance calibration scenarios. Measurement-model re-estimation, heteroskedastic or broader non-Gaussian validity, multiplicity-adjusted inference, and MGA/MICOM within this structural method are excluded. | Bootstrap/jackknife: yes; Structural Path Randomization: scoped Standard; release-qualified bounded v1 evidence with an explicit conditional/approximate interpretation warning |
| PLS-SEM | Mediation effect decomposition | Standard for the documented PLS mediation point scope and bounded General SEM multiple-mediation full-model case bootstrap. It reports direct, path-specific indirect, aggregate indirect, and total effects under exact selected-path identities. Structural Path Randomization remains a separate direct-path procedure. | Yes; bounded point and General SEM bootstrap cells with canonical effect tables and strict reopen |
| PLS-SEM | Two-stage moderation | Standard for the historical single-interaction workflow and bounded General SEM simultaneous two-way point/bootstrap cells. General SEM uses scientific rescaled gamma and fixed standardized `-1/0/+1` probes; three-way, arbitrary probes, HOC interactions, groups, and unrestricted interaction systems remain excluded. | Yes; exact point/bootstrap cells with shared native workflow and canonical moderation output |
| PLS-SEM | Control paths | Experimental v0.5 schema/output semantics; uses ordinary structural path estimation | No |
| PLS-SEM | Higher-order constructs | The historical Standard cell remains the bounded reflective-reflective disjoint two-stage point workflow. General SEM Standard adds one non-nested HOC through the exact repeated, extended-repeated, embedded two-stage, and disjoint two-stage approach/HCM matrix, with point estimates and full-model percentile case bootstrap; hybrid, interactions, multiple/nested HOCs, groups, weights, feedback, PLSc, and matrix input remain excluded. | Yes; the exact General SEM point and supplemental bootstrap cells passed independent numerical parity, consolidated Rust/frontend regression, schema-6 append/reopen, accessibility, representative soak, and Windows candidate launch gates |
| PLS-SEM | PLSc | Release-qualified scoped Standard for reflective-only recursive PLSc with at least two indicators per construct, listwise preprocessing, and path or factor weighting. It reports rho_A, attenuation-corrected construct correlations, structural paths, outer loadings, and R2. Formative blocks, PCA weighting, generated interactions, HOCs, PLSc resampling/MGA, and historical `plsc_v1` reinterpretation remain excluded. | Yes; current engine/archive/native/packaged evidence, selected-run XLSX export, and same-run save/reopen for the bounded `plsc_v2` point-estimate scope |
| PLS-SEM | WPLS | Release-qualified scoped Standard for documented positive case-weighted reflective WPLS with standardized preprocessing and path/factor weighting; WPLS inference, formative blocks, HOCs/interactions, PCA weighting, and nonpositive or missing case weights remain unsupported | Yes; current engine/archive/native/packaged evidence for the bounded `wpls_case_weighted_v1` scope |
| PLS-SEM | CCA composite residual diagnostics | Numerically validated only for descriptive observed-versus-reproduced composite-correlation residuals in recursive standardized, listwise, reflective path/factor-weighted models with at least two constructs and one path. No controls, interactions, higher-order constructs, case weights, resampling, thresholds, fit classification, or confirmatory decision is produced; this is not full SmartPLS CCA. | Supported in Standard; independent reference, browser setup/readiness, exact payload persistence, and packaged native run/XLSX export/save/reopen acceptance pass |
| PLS-SEM | CTA-PLS | Release-qualified scoped Standard for descriptive sample-covariance tetrad diagnostics on supported PLS indicator blocks with four or more indicators and exact complete-case preprocessing; inferential standard errors, p-values, intervals, bootstrap/permutation decisions, measurement classification, PCA weighting, and blocks with fewer than four indicators remain unsupported | Yes; current engine/archive/native/packaged evidence, accessible tetrad tables, selected-run export, and same-run save/reopen for the bounded `cta_pls_tetrad_v1` scope |
| PLS-SEM | Endogeneity analysis | Validated for documented Gaussian-copula diagnostic scope with rankit inverse-normal copula terms, augmented regressions, approximate t/p diagnostics, and applicability warnings; diagnostic only, not causal proof | Yes, documented Gaussian-copula diagnostic scope |
| PLS-SEM | Nonlinear effects | Validated for documented fixed-score quadratic diagnostic scope with centered squared construct-score terms, augmented-regression statistics, and R2 delta; broader nonlinear SEM remains unsupported | Yes, documented nonlinear diagnostic scope |
| PLS-SEM | Moderated mediation | Scoped Standard for exactly one selected two-relation `X → M → Y` path and one first- or second-stage two-way interaction with distinct moderator, strong hierarchy, fixed standardized probes at `-1/0/+1`, and one shared full-model case-bootstrap ledger for gamma, three conditional indirect effects, and the index. Multiple interactions, both stages moderated, longer paths, arbitrary probes, HOCs, groups, weights, and causal claims remain excluded. | Yes; Version 2.50 General SEM authoring, native calculation, five-target canonical result, export, and strict reopen |
| Prediction | PLSpredict / CVPAT | Current `plspredict_indicator_v2` scope predicts reflective endogenous indicators with train-only preprocessing, a fixed seeded balanced 10-fold plan repeated 10 times, IA and LM benchmarks, Q²_predict, RMSE/MAE/MAPE, and one-sided 95% aggregate benchmark CVPAT; construct scores and modulo-4 holdout are supplementary. The fixed plan is independently implemented, not SmartPLS-randomized, and separate saved-model comparison, formative endogenous targets, HOCs, interactions, case weights, and non-listwise missing handling remain unsupported. `plspredict_holdout_v1` is archive-readable legacy output only. | Release-qualified for the bounded indicator-level single-model benchmark scope through current method-audit and packaged Windows acceptance identities |
| Prediction | Importance-Performance Map Analysis (IPMA) | Numerically validated for the documented bounded predecessor-only importance-performance scope using PLS total effects and observed-range 0-100 performance from listwise-standardized construct/indicator scores. Theoretical-range correction, alternate SmartPLS representations, NCA integration, and cIPMA are unsupported. | Native setup/results/map, strict persistence, genuine packaged execution, XLSX export, explicit save, and same-run reopen accepted |
| Groups | PLS-POS-style diagnostic | Bounded engine preview only. `pls_pos_v1` is the frozen QuickPLS deterministic score-space routine, not a claim of unrestricted published PLS-POS equivalence. Current QuickPLS 3 factory evidence is absent pending independent reference, simulation, persistence, native/export, audit, and packaged gates. | No; preview output only |
| Groups | FIMIX-style diagnostic | Bounded engine preview only. `fimix_pls_v1` uses hard partitions, inverse-distance membership scores, and a Gaussian residual pseudo-likelihood; it is not a full finite-mixture EM engine and its scores/criteria are not posterior probabilities or full FIMIX-PLS equivalents. | No; preview output only |
| Groups | MICOM and Two-Group Permutation MGA | Supported Standard for the documented v4 scope: exactly two ordered groups, path weighting, standardized listwise data, 5,000–10,000 preplanned size-preserving partitions, exactly-once no-retry accounting, group-specific path/loading/weight re-estimation, two-tailed A-minus-B inference, and MICOM Steps 1–3 under the same partition identity. Parametric/bootstrap or one-tailed MGA, omnibus and multiplicity-adjusted tests, interactions, HOCs, case weights, consistent PLS, and broader-group claims remain excluded. | Independent fixed-plan reference, strict v4-only append validation, native setup/results, XLSX export, explicit save, and same-run packaged reopen pass |
| Groups | Historical combined MICOM/MGA v1-v3 | Retained as read-only archive identities and never relabeled as v4. The invalid historical MICOM v1 execution remains withdrawn. | No; new combined runs emit only `pls_mga_two_group_v4`, `pls_mga_permutation_v4`, and `micom_v4` |
| Assessment | Reliability, validity, structural quality, and fit diagnostics | Validated for documented assessment scope: alpha, rho_A, rho_C, AVE, cross-loadings, Fornell-Larcker, HTMT/HTMT+, VIF, R2/adjusted R2, f2, Q2, SRMR, and d_ULS; d_G, NFI, and RMS_theta remain excluded | Yes, documented assessment metrics |
| CB-SEM | CFA and maximum-likelihood SEM | Standard for the established bounded raw-data single-group reflective CFA/recursive-SEM ML workflow and the additive General SEM V3 recursive common-factor point cell. General SEM V3 consumes the resident `SemModelV4` and Parameter Table and reports typed parameter, standardized, fit, and identification rows. Mean structures, feedback, robust/ordinal/FIML estimators, groups, moderation, and broader constraint objects remain excluded. | Yes; original qualified identity is preserved, and the bounded General SEM V3 point cell uses the Version 2.50 integrated workflow |
| CB-SEM | Recursive-SEM case bootstrap | Scoped Standard for raw continuous, listwise, single-group recursive common-factor SEM with 500–10,000 full ML case refits, fixed 95% two-sided percentile Type-7 inference, a 90% usable gate, and an ordered failure ledger for eligible free parameters. Derived indirect-effect inference, BCa/studentized intervals, mean structures, feedback, groups, robust/ordinal/FIML estimators, and CB moderation remain excluded. | Yes; separate Version 2.50 cell with canonical inference/receipt tables and strict schema-6 reopen |
| Components | Standalone PCA | Validated for documented model-free PCA scope: 2–50 selected numeric raw-data variables, listwise-standardized correlation matrix, deterministic sign orientation, fixed/Kaiser/variance-threshold retention (including the threshold-crossing component), eigenvalues, loadings, weights, scores, native table/full-score export, and canonical save/reopen; rotations, pairwise deletion, covariance/correlation-only input, categorical conversion, and inference remain excluded | Yes, documented PCA scope |
| Components | GSCA | Validated for bounded `gsca_als_v2`: joint global least-squares ALS, deterministic +1 initialization, listwise-standardized raw data, disjoint reflective/formative blocks, recursive single-group paths, weights, loadings, paths, R2, objective, FIT/adjusted/local FIT, GFI, SRMR, and convergence. Controls, interactions, HOCs, case weights, multigroup analysis, score export, and inference remain excluded; `gsca_v1` is legacy preview-only. | Yes; independent global-criterion reference plus packaged native lifecycle, XLSX, strict save, and same-run reopen acceptance pass |
| Regression | OLS regression | Validated for documented packaged-native OLS scope: raw-data OLS with intercept, numeric predictors/controls, complete-case rows, HC3 robust standard errors, t statistics, p values, confidence intervals, fit diagnostics, fitted values/residuals, table-only native export, and canonical save/reopen without a phantom model; HC0/HC4 claims, categorical encoding, survey weights, clustered SEs, GLS, mixed models, and panel models remain excluded | Yes, documented OLS scope |
| Regression | Binary logistic regression | Current `regression_logistic_v2` is validated for an exactly coded numeric 0/1 outcome with both classes represented in the listwise-complete sample, deterministic single-worker Newton IRLS, Wald SE/z/p, odds ratios and intervals, likelihood diagnostics, convergence output, fitted probabilities, and explicitly in-sample descriptive classification. Multinomial, ordinal, weighted, clustered, Firth-corrected, and out-of-sample classification claims remain unsupported; `regression_logistic_v1` is archive-only. | Release-qualified for the bounded v2 scope: independent Python/R comparison, strict persistence, browser setup, genuine packaged execution, XLSX, explicit save, and same-run reopen pass |
| Regression | OLS and binary-logistic bootstrapping | Current `regression_bootstrap_v1` uses seeded indexed case resampling with replacement, Type-7 percentile intervals as primary output, conditional all-delete-one BCa intervals, bootstrap SE and a documented two-sided standard-normal bootstrap-ratio test. Engine bounds are 99–10,000 replicates; UI guidance is 1,000 exploratory and 10,000 final. Studentized/custom-tail/custom-alpha, PROCESS, weights, multinomial, and ordinal inference remain excluded. | Release-qualified for the bounded v1 scope: independent Python/R reference, exact failure-boundary tests, strict append/tamper/save/reopen validation, three-viewport browser acceptance, genuine packaged OLS and logistic 10,000-resample execution, native XLSX exports, cancellation, explicit save, and same-run reopen pass |
| Regression | Graph-defined Path Analysis and PROCESS | Current `regression_process_v2` provides independently implemented graph-defined continuous-outcome OLS equations, parallel/serial mediation, continuous or exact-0/1 moderation, direct-path three-way moderation, one first- or second-stage moderated edge per indirect path, conditional indirect effects and indices, HC3/Student-t simple slopes and Johnson-Neyman regions, and optional dedicated percentile/conditional-BCa case bootstrap. The bounded graph supports at most eight predictors, one control, four mediators, two reusable moderators, sixteen paths, four moderated paths, and fifty generated terms per equation. Binary outcomes, weights/clusters, copied numbered templates, intermediate or multiple moderated stages on one indirect path, studentized/custom-tail/custom-alpha inference, and broader categorical coding remain excluded. Historical `regression_process_v1` is archive-only. | Supported in Standard for the bounded v2 point-estimation and dedicated bootstrap scope: independent Python/R reference, exact scientific and archive-tamper boundaries, responsive browser acceptance, genuine packaged 10,000-resample execution, cancellation/retry, accessible result and plot-data tables, XLSX export, explicit save/reopen, repeated-completion identity, terminal process-role stability, and clean shutdown pass. |
| NCA | CE-FDH and CR-FDH | `nca_v2` is numerically and archive qualified for one observed numeric X/Y pair, record-high CE-FDH peers, CR-FDH regression through those peers, seeded permutation p values, and observed-range bottlenecks. Multiple conditions/outcomes, latent-score NCA, cIPMA integration, categorical encoding, theoretical ranges, and broader ceilings remain unsupported. `nca_v1` is legacy archive compatibility only. | Independent reference, strict persistence, packaged native setup/results/XLSX/export/reopen, and no-phantom-model acceptance pass |

Publication audit update: `publication_ready_v0_1_to_v0_8` now clears for the documented supported scope. Rows that still say experimental describe broader or unsupported behavior outside that audited scope; they do not override the method-specific publication audit artifacts.

The desktop catalog and Rust core share this policy. A method name in the product roadmap is not evidence of implementation or validation outside its documented scope.

Detailed release gates and next actions are machine-readable in `D:\QuickPLS\validation\development_slices.json` and available with:

```powershell
cargo run -p qpls-cli -- roadmap
cargo run -p qpls-cli -- gate v0_4_assessment_reliability
```
