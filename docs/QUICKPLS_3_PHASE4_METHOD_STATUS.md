# QuickPLS 3 Phase 4 method status

Snapshot: 2026-08-14. This ledger distinguishes current frozen-source evidence from packaged Windows release evidence. It is not a SmartPLS parity claim.

## Release-qualified in this Phase 4 lane

| Capability | Current bounded state | Evidence limit | Release gap |
| --- | --- | --- | --- |
| Indicator-level PLSpredict and CVPAT benchmarks v2 | Release-qualified | Independent indicator/fold/IA/LM/CVPAT oracle; deterministic and boundary tests; strict archive tamper tests; native setup, Results, accessibility, same-run export, exact packaged viewport, cancel/retry, save/reopen, offline-monitoring, and cleanup evidence | None for the bounded single-model benchmark scope; saved-model comparison remains a separate absent capability |
| Prospective PLS sample-size and power v2 | Scoped Standard successor | Null-centered two-sided indexed case-bootstrap tail probability with plus-one correction, typed no-retry failure accounting, deterministic seeded Monte Carlo grids, strict archive/native/export contracts, and a separately versioned identity | None for the bounded two-reflective-construct, one-path Gaussian/no-missing design; broader models, distributions, and missing-data designs remain excluded |

Source-only evidence did not qualify PLSpredict/CVPAT. Its current release state additionally binds genuine packaged proof for all declared viewports, invalid setup, execution, cancel/retry, result inspection, same-run CSV/XLSX export, save, close, reopening the same run, offline monitoring, and process cleanup. It does not qualify pairwise saved-model comparison or prediction-oriented model selection.

## Historical audited gaps from the 14 August snapshot

The table below is retained as the pre-promotion audit trail. Several entries were subsequently promoted under newer identities; it is not the current availability list. Use Capability Registry V2 and each active method manifest for current status.

| Capability | Current state | Exact blocker before promotion |
| --- | --- | --- |
| Coupled MICOM v3 and permutation MGA v3 | Implemented candidate; qualification absent | The v3 engine now uses a canonical unordered-group pool and complementary label plan, preserves exact A/B signed reversal, failure accounting, p values, seed behavior, and worker behavior, and retains v2 archives as historical-only. Final independent qualification identities, rebuilt native evidence, and packaged workflow evidence are still absent, so the strict factory remains fail-closed. |
| Historical PLS-SEM sample-size and power analysis v1 | Read-only historical identity; null calibration failed | The bounded prospective two-construct Gaussian engine, typed archive contract, native setup/results/export, deterministic streams, workload caps, and independent Python/R source exist. Exact qualification completed with zero fit failures but rejected 190/2,500 null datasets (7.60%; 95% Wilson 6.625%-8.705%), so the case-bootstrap normal-reference contract is anti-conservative and its manifest remains historical and absent. The separately versioned v2 successor uses null-centered two-sided plus-one inference and is the current scoped Standard workflow; v1 thresholds, seeds, payloads, and saved results are not tuned or relabeled. |
| PLSc consistent bootstrap | Absent | The current validated PLSc scope rejects resampling settings. Full replicate-level PLSc re-estimation, failure accounting, intervals, persistence, and native workflow are missing. |
| PLSc consistent permutation | Absent | No PLSc group-permutation engine or PLSc-aware invariance hierarchy exists; current PLSc scope rejects permutation settings. |
| PLSc consistent MGA | Absent | Depends on the missing consistent bootstrap and consistent permutation methods and has no independent engine or native workflow. |
| Pairwise saved PLS-model comparison | Absent | Current CVPAT compares one fitted PLS model with IA/LM benchmarks and explicitly does not compare separately saved models. Common-fold saved-model identities and paired comparison persistence are missing. |
| Prediction-oriented model selection | Absent | No nested cross-validation engine, candidate-model identity contract, selection payload, persistence, or native workflow exists. Benchmark CVPAT must not be relabeled as model selection. |
| PLS model fit v2 | Partial coverage; evidence absent | Point-fit v2 reports saturated and estimated SRMR, d_ULS, d_G, Chi-square, df, and NFI. The bounded Labs `pls_model_fit_exact_v1` path now adds separately null-transformed saturated and estimated fixed-index full-refit ledgers, Type-7 HI95/HI99 bounds, decisions, failure accounting, cancellation, strict project/native projections, and canonical native exports. It remains non-promoted because full supported-shape breadth, independent full-refit oracle coverage, pre-registered simulation calibration, real final-draw archive/all-format readback, packaged Windows/accessibility/performance evidence, and independent scientific review are absent. |
| CCA bootstrap decision workflow | Absent beyond release-qualified descriptive CCA | `cca_composite_residual_v1` is release-qualified only for descriptive composite residuals. Bootstrap discrepancy tests and broader CCA decision rules remain explicitly unsupported. |

## Promotion rule

A method moves only when its manifest derives the declared state from current identity-bound evidence. An existing calculation, UI label, old report, or packaged screenshot cannot substitute for missing scientific, persistence, native, or release roles.
